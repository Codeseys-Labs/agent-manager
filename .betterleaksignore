# betterleaks allowlist — paths whose "secrets" are deliberate test fixtures or
# documentation examples, NOT real credentials. Every entry below was reviewed:
# the strings are fake tokens (ghp_abcdef…, user:p4ss@host, hunter2…) that EXIST
# to exercise the credential-redaction code (src/lib/redact.ts, getStatus URL
# scrubbing, the web /api/config + /api/sync redactors, secrets rewrap/revoke).
# Removing them would delete the very tests that prove we don't leak real
# secrets. GitGuardian flagged 8 of these on PR #42 — they are false positives.
#
# Format: one path glob per line (betterleaks treats these as paths to skip).
# Keep this list TIGHT — only test/example files, never a real source path that
# could carry a live secret. New real-secret findings outside these paths MUST
# still block.

# Redaction unit tests — fake tokens by design.
test/lib/redact.test.ts

# Git-remote credential-scrubbing tests (fake ghp_/user:pass URLs).
test/core/git.test.ts

# MCP am_status/am_doctor + scope tests (fake ghp_ remote URLs).
test/mcp/server.test.ts

# Web /api/config + /api/sync redaction tests (fake credential URLs).
test/web/sync-push-pull.test.ts
test/web/config-redaction.test.ts

# Secrets rewrap/revoke tests (fake age envelopes / passphrases).
test/commands/secrets-rewrap.test.ts
test/commands/secrets-revoke.test.ts

# Source files whose COMMENTS/examples document credential formats (the example
# `https://x-access-token:ghp_xxx@…` strings in docstrings, not live secrets).
src/core/git.ts
src/lib/redact.ts
