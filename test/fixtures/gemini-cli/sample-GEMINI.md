# Project Instructions

This project uses TypeScript with strict mode enabled.

## Coding Standards

- Use async/await over raw promises
- All functions must have explicit return types
- Use Bun runtime APIs where possible
