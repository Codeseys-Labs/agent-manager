{
  "summary": "Deep research for research-heavy backlog items: Zod discriminatedUnion migration (b5/a067), testable-clock seam (b6), release/CHANGELOG best practices",
  "agentCount": 4,
  "logs": [],
  "result": {
    "reports": [
      {
        "topic": "Migrating am ServerSchema from z.object+superRefine to z.discriminatedUnion on transport (seed agent-manager-a067), Zod 3.25.76",
        "keyFindings": [
          "Installed zod is 3.25.76 (package.json says ^3.24.4, node_modules resolves 3.25.76). This is the v4-preview-backported v3 line: scope all findings here, NOT older 3.x. Verified empirically by writing throwaway probes against the repo's own zod.",
          "DEFAULT-DISCRIMINATOR SHARP EDGE IS REAL AT RUNTIME: a bare discriminatedUnion('transport', [...]) where the stdio literal carries .default('stdio') still THROWS 'No matching discriminator' when parsing {command:'x'} with transport absent. Proven: Union.parse({command:'x'}) throws invalid_union/'No matching discriminator'. The union resolver inspects raw input BEFORE field-level defaults apply, so .default() on the literal does not save you. You MUST inject the discriminator before the union runs.",
          "IDIOMATIC FIX (verified working in 3.25.76): wrap the union in z.preprocess that injects transport:'stdio' when absent. const ServerSchema = z.preprocess((v)=> v && typeof v==='object' && (v as any).transport===undefined ? {...(v as object), transport:'stdio'} : v, z.discriminatedUnion('transport', [Stdio, Remote])). All 6 existing behavioral assertions pass with this: minimal{command}->stdio default; explicit stdio; remote+url; stdio+url REJECTED; empty REJECTED; bad transport REJECTED.",
          "TYPECHECK: In 3.25.76 the issue-#1490 TS error (ZodDefault on discriminator literal rejected by discriminatedUnion's type constraint) is GONE — both `transport: z.literal('stdio').default('stdio')` and a plain `z.literal('stdio')` typecheck cleanly inside discriminatedUnion. So .default() on the literal is optional/harmless for types, but irrelevant for runtime (preprocess is what makes it work). Recommend OMIT .default() on the literal and rely solely on preprocess — matches the canonical coder/mux pattern and avoids confusion.",
          "ADAPTER-CHURN FEAR IS LARGELY UNFOUNDED. The 13 adapter export paths consume ResolvedServer (hand-written interface in src/core/resolved.ts, command:string always present, url?:string), NOT z.infer<typeof ServerSchema>. ResolvedServer is fully decoupled from the Zod type, so discriminatedUnion narrowing of z.infer<Server> does NOT propagate into any adapter. Confirmed: zero adapter files reference the `Server` z.infer type; they use ResolvedServer / ImportedServer / NativeServer.",
          "THE REAL TS CHOKEPOINT IS A SINGLE FILE: src/core/config.ts buildResolvedConfig (lines 213-224) reads srv.command and srv.url to build ResolvedServer. Under the union, srv.command exists on both variants (fine) but srv.url only on the remote variant. Because `command` is kept on BOTH variants (compat strategy below), srv.command typechecks; srv.url over the union infers string|undefined which assigns cleanly to ResolvedServer.url?:string. VERIFIED: the bridge access {const cmd:string=srv.command; const url:string|undefined=srv.url} typechecks with no error when command is on both variants.",
          "EXACTLY TWO object-literal-then-mutate WRITE sites break: install.ts:156-174 and marketplace/installer.ts:157-172. Both build a {command, transport:<broad-union-var>, ...} literal then conditionally do `server.url = ...`. VERIFIED this fails: TS2322 'Type string is not assignable to type undefined' because a broad transport union type cannot narrow to the remote variant, so url is seen as the stdio variant's z.undefined(). Fix: construct the remote object with url already present (branch on transport) instead of post-hoc mutation.",
          "add.ts:154 and init-project.ts:101 are SAFE: they use a literal transport:'stdio' so TS picks the stdio variant, and they only mutate shared-base fields (args/tags/env/description), never .url. No change needed.",
          "BEHAVIORAL CONTRACT to preserve (test/core/schema.test.ts): (1) parse({command}) -> transport='stdio', enabled=true; (2) rejects {} (no command); (3) rejects transport='grpc'; (4) rejects stdio+url; (5) ACCEPTS remote with command holding the URL AND a url field set simultaneously (line 135-143) — the remote variant MUST keep command valid. The _registry XOR _marketplace superRefine (schema.ts:64-72) must be preserved by re-applying it via .superRefine on the union or on the shared base.",
          "REAL-WORLD CONFIRMATION: coder/mux (src/common/orpc/schemas/mcp.ts) uses exactly this — a strict z.discriminatedUnion('transport',[stdio->command, http/sse->url]) for the resolved type, PLUS a separate input schema with transport.optional()+superRefine normalizing `transport = input.transport ?? 'stdio'` for the wire format. huggingface.js tiny-agents (packages/tiny-agents/src/lib/types.ts) uses z.discriminatedUnion('type',[stdio->command, http/sse->url]) with no default. Both keep command exclusively on stdio and url on remote — am differs because it historically stores the URL in `command`, so am must keep command on the remote variant too."
        ],
        "recommendedApproach": "EXACT UNION SHAPE (drop-in for src/core/schema.ts, Zod 3.25.76):\n\n  // Shared base = every field common to both transports, so z.infer keeps them present on both variants (zero adapter/bridge churn).\n  const ServerBase = z.object({\n    command: z.string(),                                  // KEPT ON BOTH variants — am stores the URL in `command` for remote (see export-utils.ts:95 `entry.url = server.command`). This is the whole reason a naive union breaks adapters; keeping command on both eliminates it.\n    args: z.array(z.string()).optional(),\n    env: z.record(z.string(), z.string()).optional(),\n    description: z.string().optional(),\n    tags: z.array(z.string()).optional(),\n    enabled: z.boolean().default(true),\n    _registry: RegistryProvenanceSchema.optional(),\n    _marketplace: MarketplaceProvenanceSchema.optional(),\n    adapters: adaptersPassthrough,\n  });\n\n  const StdioServer = ServerBase.extend({\n    transport: z.literal(\"stdio\"),        // NO .default() here — preprocess handles the default; keeps the discriminator a plain literal (matches coder/mux & hf.js).\n    url: z.undefined().optional(),        // forbids url on stdio at the type AND runtime level — replaces the superRefine stdio+url check exactly. (z.undefined() is an allowed discriminatedUnion member type per docs.)\n  });\n\n  const RemoteServer = ServerBase.extend({\n    transport: z.union([z.literal(\"streamable-http\"), z.literal(\"sse\")]),\n    url: z.string().optional(),           // url allowed (optional) on remote; command still carries the URL today.\n  });\n\n  export const ServerSchema = z.preprocess(\n    (v) => (v && typeof v === \"object\" && (v as Record<string, unknown>).transport === undefined\n      ? { ...(v as object), transport: \"stdio\" }\n      : v),\n    z.discriminatedUnion(\"transport\", [StdioServer, RemoteServer]).superRefine((data, ctx) => {\n      // preserve the _registry XOR _marketplace invariant (was schema.ts:64-72)\n      if (data._registry && data._marketplace) {\n        ctx.addIssue({ code: z.ZodIssueCode.custom, path: [\"_marketplace\"],\n          message: \"_registry and _marketplace are mutually exclusive: a server is either registry-installed or marketplace-imported, not both\" });\n      }\n    }),\n  );\n  export type Server = z.infer<typeof ServerSchema>;\n\nDEFAULT-DISCRIMINATOR HANDLING: do it in the z.preprocess step (injects transport:'stdio' when the key is absent), NOT via .default() on the literal — verified that .default() alone leaves the union throwing 'No matching discriminator' at runtime. The stdio+url rejection is now structural (z.undefined() on stdio.url), so that branch of the old superRefine is deleted; only the registry/marketplace XOR refine is re-attached.\n\nADAPTER-COMPAT STRATEGY (minimizes churn to ~1-3 files):\n1. Keep `command` and all common fields in ServerBase so z.infer<Server> has command/args/env/description/tags/enabled/_registry/_marketplace/adapters present on BOTH variants. Only `url` (and the literal transport) differ per variant. Net effect: every existing READ of server.command / server.args / server.env / server.tags / server.description / server.enabled keeps typechecking unchanged across the whole codebase.\n2. The adapters need ZERO changes: they consume ResolvedServer (src/core/resolved.ts), which is hand-written and unaffected. Leave resolved.ts as-is.\n3. Fix exactly buildResolvedConfig in src/core/config.ts (213-224): it already reads srv.command (on both variants → fine) and srv.url (string|undefined over union → assigns to ResolvedServer.url?:string → fine). VERIFIED this typechecks unchanged. No edit likely needed; confirm with `bun run scripts/typecheck.ts`.\n4. Fix the 2 mutate-after-literal sites by constructing the final shape directly:\n   - install.ts (156-174): replace the literal + `if(...) server.url=...` with a transport-branched constructor, e.g. build `const base = {command, args, transport, enabled, description, tags, ...(env?{env}:{}), _registry}` and for remote spread `...(transport!=='stdio' && pkg.server.url ? {url: pkg.server.url} : {})` into the literal so url is present at construction. Or assemble as `Record<string,unknown>` then `ServerSchema.parse(obj)`.\n   - marketplace/installer.ts (157-172): same — fold `if(serverDef.url) server.url=...` into the object literal via a conditional spread before assignment.\n5. add.ts:154 and init-project.ts:101 need no change (literal 'stdio', mutate only shared fields).\n6. secret-detection.ts and mcp-superset.ts take structural / McpServerShape params (not Server), immune.\n7. Tests: test/core/schema.test.ts assertions all pass as-is with this shape (verified the 6 behavioral cases). The 'rejects url on stdio' message changes from the superRefine custom string to a Zod invalid_type on `url` (expected undefined) — if a test asserts the exact message it must be loosened to assert `.toThrow()` (line 125-133 only asserts toThrow, so it passes). web/server.ts safeParse sites already pass transport explicitly and continue to work; preprocess makes them robust to omission.\n\nVERIFICATION GATE before merge: `bun run scripts/typecheck.ts` (full), `bun test test/core/schema.test.ts`, `bun test test/core test/adapters`, and `bun test test/commands/install.test.ts`.",
        "pitfalls": [
          "DO NOT rely on `transport: z.literal('stdio').default('stdio')` inside the union for the default behavior — it typechecks in 3.25.76 but at RUNTIME the union still throws 'No matching discriminator' for input with transport absent (the resolver reads raw input before defaults). The default MUST be injected by z.preprocess before the union. This is the single most likely way to ship a broken migration that passes typecheck but fails on real config.toml files lacking [servers.x] transport.",
          "DO NOT move `command` out of the remote variant. am stores the URL in `command` for remote transports (export-utils.ts:95 `entry.url = server.command`; copilot/codex/kilo-code export the same way; mcp-superset.ts:112 and url-credentials.ts:107 test server.command for http(s)). A 'pure' union with command only on stdio would break all of these AND the 'accepts url on a remote transport' test (schema.test.ts:135) which sets command to the URL. Keep command in the shared base.",
          "DO NOT forget to re-attach the _registry XOR _marketplace superRefine — it lives at schema.ts:64-72 today and has its own test (schema.test.ts:91 'rejects both'). z.discriminatedUnion(...).superRefine(...) works; attach it on the union (inside the preprocess) so it runs after discrimination.",
          "z.preprocess wrapping changes the schema's outer type from ZodObject to ZodEffects. If any code does ServerSchema.shape, .extend(), .merge(), .pick(), or .omit(), it will break (ZodEffects has no .shape). Verified current usage: only .parse / .safeParse are called on ServerSchema (install/web/tests) plus z.record(z.string(), ServerSchema) in ConfigSchema/ProjectConfigSchema — all of which accept ZodEffects fine. If future code needs .shape, expose the inner discriminatedUnion separately.",
          "The stdio+url rejection error MESSAGE changes (from the custom superRefine string to Zod's invalid_type 'Expected undefined, received string' on path ['url']). The existing test only asserts `.toThrow()` so it passes, but any downstream code or test asserting the exact old message ('url is not valid for a stdio transport...') must be updated. Search before merge: grep for that string.",
          "install.ts and marketplace/installer.ts use the object-literal-then-`.url=`-mutation antipattern that breaks under the union (TS2322, verified). Resist the temptation to silence with `as Server` / `as any` — that reintroduces the very bug the schema prevents (a stdio literal getting a url at runtime). Construct the correct discriminated shape up front via conditional spread or parse a plain object.",
          "Don't assume buildResolvedConfig needs a rewrite — verified its srv.command/srv.url access typechecks unchanged with the shared-base design. Over-refactoring resolved.ts or the adapters is wasted churn and risk; the migration is genuinely ~1 schema edit + 2 small constructor fixes, not the 13-adapter rewrite the seed comment implies.",
          "z.preprocess receives `unknown`; the injector must guard `v && typeof v === 'object'` before spreading, or non-object inputs (e.g. a stray string in TOML) will throw a TypeError instead of yielding a clean Zod validation error."
        ],
        "sources": [
          "/mnt/e/CS/github/agent-manager/package.json (zod ^3.24.4) and node_modules/zod/package.json (resolved 3.25.76)",
          "/mnt/e/CS/github/agent-manager/src/core/schema.ts:35-86 (current ServerSchema + superRefine)",
          "/mnt/e/CS/github/agent-manager/src/core/resolved.ts:8-19 (ResolvedServer interface — decoupled from z.infer<Server>)",
          "/mnt/e/CS/github/agent-manager/src/core/config.ts:206-225 (buildResolvedConfig bridge — the single TS chokepoint)",
          "/mnt/e/CS/github/agent-manager/src/adapters/shared/export-utils.ts:49-99 (entry.url = server.command invariant; remote stores URL in command)",
          "/mnt/e/CS/github/agent-manager/src/commands/install.ts:155-175 and src/marketplace/installer.ts:157-172 (the 2 mutate-after-literal sites that break)",
          "/mnt/e/CS/github/agent-manager/src/commands/add.ts:154-168 and src/commands/init-project.ts:101 (safe literal-'stdio' construction sites)",
          "/mnt/e/CS/github/agent-manager/test/core/schema.test.ts:20-144 (behavioral contract that must be preserved)",
          "Empirical probes run against repo's zod 3.25.76: confirmed (a) preprocess+union passes all 6 behavioral assertions, (b) bare union with .default literal throws 'No matching discriminator' at runtime on transport-absent input, (c) .default on discriminator literal typechecks in 3.25.76, (d) install.ts mutate-url pattern fails TS2322 under the union",
          "Zod issue colinhacks/zod#1490 (ZodDefault on discriminator rejected by discriminatedUnion type constraint — older 3.x, closed not-planned; resolved in the 3.25/v4 line)",
          "Zod v4 test discriminated-unions.test.ts:354-373 (discriminator literals may carry .default()/.transform()/.pipe()) via Context7 /colinhacks/zod",
          "Zod official api.mdx via Context7: discriminator must be z.enum/z.literal/z.null/z.undefined; nested discriminated unions supported",
          "Real-world: coder/mux src/common/orpc/schemas/mcp.ts (strict discriminatedUnion('transport') for resolved + separate optional-transport input schema with superRefine normalizing transport ?? 'stdio')",
          "Real-world: huggingface/huggingface.js packages/tiny-agents/src/lib/types.ts (discriminatedUnion('type', [stdio->command, http/sse->url]), no default)"
        ]
      },
      {
        "topic": "Injectable clock/time seam for deterministic TTL testing of the registry LRUCache in agent-manager (seed 06d8)",
        "keyFindings": [
          "EXACT TARGET: src/registry/client.ts lines 10-38 define a private `class LRUCache<T>` that reads the wall clock inline at TWO sites — line 20 `if (Date.now() > entry.expiresAt)` (get/expiry check) and line 36 `this.map.set(key, { value, expiresAt: Date.now() + this.ttlMs })` (set/stamp). Line 53 instantiates a single module-level singleton `const cache = new LRUCache<unknown>(CACHE_MAX_ENTRIES, CACHE_TTL_MS)`. LRUCache is NOT exported and has zero consumers outside this file (only references are the singleton on L53 and doc comments in the test), so adding a constructor param is a safe internal-only change with no call-site churn.",
          "CURRENT WORKAROUND TO REPLACE: test/registry/client-resilience.test.ts already exercises TTL expiry by monkey-patching the global `Date.now` (L76 snapshots `originalDateNow = Date.now`, L97 restores it in afterEach, and the test at L211-235 does `let fakeNow = 1_000_000; Date.now = () => fakeNow;` then mutates `fakeNow`). The file header (L24-31) explicitly flags this as the missing seam and names seed agent-manager-06d8. This is the anti-pattern the seam removes: reassigning a global builtin, restore-in-afterEach fragility, and process-global cache-key collisions forcing a per-test unique base URL hack (L70-73).",
          "CANONICAL PATTERN (constructor-injected clock): add a third constructor param `now: () => number = Date.now` defaulting to the system clock, store it (`private now`), and replace both `Date.now()` call sites with `this.now()`. Default binding means production code and all existing call sites are untouched — `new LRUCache(max, ttl)` still works because `Date.now` is the default. This is the industry-standard 'clock as a dependency' / 'now-function' seam and is strictly cleaner and more portable than fake timers because it has zero global state and zero reliance on test-runner magic.",
          "lru-cache (isaacs/node-lru-cache, the most-downloaded TS LRU): DOES expose an injectable clock via the constructor option `perf?: { now(): number }` (default `defaultPerf`). Internally it wraps it as a local `getNow = () => this.#perf.now()` and calls it from the TTL closures (`#isStale`, `#setItemTTL`, etc.). README verbatim: 'you can pass in a `perf` option when creating your LRUCache instance. This option accepts any object with a `now` method that returns a number' and warns it 'creates an internal reference to the global performance or Date objects at import time ... those references cannot be mocked'. So the respected library settled on exactly the inject-a-now()-function seam — but as an OBJECT `{ now(): number }`, not a bare function.",
          "quick-lru (sindresorhus): does NOT expose any clock injection. Its only constructor options are `maxSize` (required), `maxAge` (optional TTL ms), and `onEviction`. It calls `Date.now()` directly at expiry-stamp (`Date.now() + maxAge`) and expiry-check (`item.expiry <= Date.now()`). To test TTL with quick-lru you must monkey-patch the global — confirming that a library WITHOUT the seam forces exactly the anti-pattern this seed is removing.",
          "Bun fake-timer support (verified empirically on Bun 1.3.14 in this repo, and from bun.sh/docs/test/time): `import { setSystemTime } from 'bun:test'` IS supported and DOES mock `Date.now()`, `new Date()`, and `Intl.DateTimeFormat`. Signature: `setSystemTime(now?: Date | number): void`; call `setSystemTime()` with no args to reset to the real clock. Jest-compat `jest.useFakeTimers()/useRealTimers()/setSystemTime()/now()` also work. CRITICAL CAVEAT from docs: 'Timers are not impacted yet' — `setTimeout`/`setInterval` are NOT mocked by setSystemTime, so it only helps the Date-based TTL, not the backoff sleeps (the test already stubs setTimeout separately for those). Note Bun (unlike Jest) keeps `Date === OriginalDate` under fake timers.",
          "PORTABILITY VERDICT: dependency injection (the now()-param seam) is cleaner and more portable than setSystemTime for this case. setSystemTime mutates a runner-global system clock for the whole test scope (must be reset, can leak between tests, is Bun-specific, and does not touch timers). The injected clock is local to one LRUCache instance, requires no afterEach restore, works in any runner, and lets a test advance time by mutating a captured `let` — the same ergonomics lru-cache's own `perf: { now: () => myClockTime }` test recipe uses."
        ],
        "recommendedApproach": "Use a CONSTRUCTOR-INJECTED now() function defaulting to the system clock (a setter is unnecessary — the cache is constructed once at module load and time is fixed for the instance's life). Minimal seam, three edits in src/registry/client.ts, no call-site changes:\n\n1) Constructor (lines 12-15) — add a defaulted third param and store it:\n```ts\nconstructor(\n  private maxSize: number,\n  private ttlMs: number,\n  private now: () => number = Date.now,\n) {}\n```\nUse `() => number` (bare function), not lru-cache's `{ now(): number }` object — simpler, and there is no second caller to satisfy. `= Date.now` keeps production behavior identical and means the L53 singleton `new LRUCache(CACHE_MAX_ENTRIES, CACHE_TTL_MS)` compiles unchanged.\n\n2) get() expiry check (line 20): `if (Date.now() > entry.expiresAt)` → `if (this.now() > entry.expiresAt)`\n3) set() stamp (line 36): `expiresAt: Date.now() + this.ttlMs` → `expiresAt: this.now() + this.ttlMs`\n\nTo make it testable, EXPORT the class so a test can build its own instance with a fake clock: `export class LRUCache<T>`. This is preferable to also exporting the module singleton — keep `cache` private; tests construct a fresh instance and assert get/set/TTL/eviction directly, avoiding the process-global cache-key-collision hack the resilience test currently needs.\n\nDETERMINISTIC bun:test (no globals touched, no afterEach restore for the clock):\n```ts\nimport { describe, expect, test } from \"bun:test\";\nimport { LRUCache } from \"../../src/registry/client\";\n\ndescribe(\"LRUCache TTL (injected clock)\", () => {\n  test(\"entry expires exactly at ttlMs and is gone after\", () => {\n    let clock = 1_000_000;\n    const cache = new LRUCache<string>(10, 5 * 60 * 1000, () => clock);\n\n    cache.set(\"k\", \"v\");                 // expiresAt = 1_000_000 + 300_000\n    expect(cache.get(\"k\")).toBe(\"v\");    // t=1_000_000, fresh\n\n    clock += 5 * 60 * 1000;              // t == expiresAt; guard is `>` so still valid\n    expect(cache.get(\"k\")).toBe(\"v\");\n\n    clock += 1;                          // strictly past TTL → expired + deleted\n    expect(cache.get(\"k\")).toBeUndefined();\n  });\n});\n```\nThis pins the boundary (the L20 guard is strict `>`, so the entry survives at exactly expiresAt and dies one ms later — the test asserts both edges).\n\nAfter landing the seam, REWRITE the existing TTL test in test/registry/client-resilience.test.ts (L211-235): for an end-to-end test through getVersions() that still needs to drive the module singleton's clock, prefer `setSystemTime` over reassigning `Date.now` — change L217 `Date.now = () => fakeNow` to `setSystemTime(new Date(fakeNow))` and L97 `Date.now = originalDateNow` to `setSystemTime()` (reset). But the cleaner long-term move is to delete that integration-level TTL case and cover TTL via the direct LRUCache unit test above, leaving the resilience file to cover only network/retry/fallback/eviction. Keep the separate setTimeout stub for backoff — setSystemTime does not mock timers.\n\nIf you ever swap the hand-rolled cache for isaacs/lru-cache, mirror their API by passing `perf: { now: () => clock }` instead — but the built-in cache does not warrant that dependency.",
        "pitfalls": [
          "Do NOT reach for setSystemTime as the PRIMARY seam — it is the fallback for code you cannot refactor. Here you own LRUCache, so inject the clock; setSystemTime leaves global state that must be reset and silently leaks into later tests if an afterEach is missed (the current file's L97 restore exists precisely because of this hazard).",
          "setSystemTime does NOT mock setTimeout/setInterval (Bun docs: 'Timers are not impacted yet'). Do not delete the existing globalThis.setTimeout stub (L80-86) thinking a time mock covers the backoff sleeps — the RETRY_DELAYS (1s/2s/4s) sleeps still need that separate synchronous-fire stub.",
          "Boundary off-by-one: the expiry guard is strict `>` (`this.now() > entry.expiresAt`), so an entry is STILL VALID at exactly expiresAt and only expires one unit later. A naive test that advances by exactly ttlMs and expects a miss will fail. Advance by `ttlMs + 1` to cross it (the existing test gets this right with `+ 1`).",
          "Default-param binding subtlety: write `private now: () => number = Date.now` (pass the reference), NOT `= () => Date.now()`. Both work, but binding the reference is the canonical form and avoids an extra closure allocation per instance. Either way, capture is at construction time, which is fine since the singleton is built once.",
          "lru-cache's documented import-time capture footgun does NOT apply to the hand-rolled cache, but DO heed it if you migrate: isaacs/lru-cache grabs a reference to global Date/performance at import time, so `setSystemTime`/Date mocking fails unless you dynamically import the module after the mock, OR you pass the `perf` option (which is why they added it). The injected-clock seam sidesteps this entirely.",
          "Don't export the module-level `cache` singleton to test TTL through it — that reintroduces the process-global cache-key-collision problem the resilience test works around with a per-test unique AM_REGISTRY_URL counter (L70-73). Export the CLASS and build fresh instances in tests instead.",
          "Bun-specific: under fake timers `Date === OriginalDate` and `Date.now === OriginalDate.now` (unlike Jest, which swaps the constructor). Tests that assert identity of Date across the mock boundary behave differently than they would in Jest — relevant only if you port Jest tests in.",
          "Keep the change strictly additive: the third constructor param MUST be optional/defaulted so the L53 production instantiation and any future caller using `new LRUCache(max, ttl)` keep compiling. Adding it as a required param would be a breaking signature change for no benefit."
        ],
        "sources": [
          "src/registry/client.ts (repo) — LRUCache class L10-38 (Date.now at L20 and L36), singleton at L53; LRUCache not exported, no external consumers",
          "test/registry/client-resilience.test.ts (repo) — header missing-seam note L24-31 citing seed 06d8; Date.now monkey-patch at L76/L97/L217; TTL test body L211-235; setTimeout stub L80-86",
          "Bun docs — Dates and times (https://bun.sh/docs/test/time) — setSystemTime signature/reset, mocks Date.now/new Date/Intl, 'Timers are not impacted yet', jest.useFakeTimers/useRealTimers/now, TZ handling",
          "Empirical verification: `bun test` on Bun 1.3.14 in this repo — confirmed `import { setSystemTime } from 'bun:test'` mocks Date.now() (1 pass)",
          "node_modules/bun-types/test.d.ts:89 — `export function setSystemTime(now?: Date | number): ThisType<void>;` (installed Bun types confirm the signature)",
          "isaacs/node-lru-cache src/index.ts (https://raw.githubusercontent.com/isaacs/node-lru-cache/main/src/index.ts) — `perf?: Perf` option where `Perf = { now(): number }`, default `defaultPerf`, internal `getNow = () => this.#perf.now()`",
          "isaacs/node-lru-cache README (https://github.com/isaacs/node-lru-cache/blob/main/README.md) — verbatim `perf` option docs + import-time clock-capture warning + 'dynamically import the package within your tests' guidance",
          "sindresorhus/quick-lru index.js (https://raw.githubusercontent.com/sindresorhus/quick-lru/main/index.js) — options maxSize/maxAge/onEviction only; calls Date.now() directly at `Date.now() + maxAge` and `item.expiry <= Date.now()`; no clock injection"
        ]
      },
      {
        "topic": "Release engineering for agent-manager (am): a tag-triggered Bun-compiled CLI shipped via GitHub Releases + curl|sh installer (npm deferred) — RELEASING.md runbook content + a release.yml version-assert step",
        "keyFindings": [
          "NO VERSION-ASSERT EXISTS, AND THE BUILD DOESN'T USE package.json. In release.yml the build derives `VERSION=\"${GITHUB_REF_NAME#v}\"` (line 33) and bakes it into the binary via `--define process.env.BUILD_VERSION` (build.ts:124) and into `src/lib/version.ts` (`AM_VERSION = process.env.BUILD_VERSION`). package.json `version` is only synced LATE (release.yml:279-286, after build, for the disabled npm step) and never compared to the tag. Result: a binary can ship correctly labeled by the tag while package.json/CHANGELOG disagree, and the human bump in scripts/bump-version.sh is never verified against the tag that triggered the run. A pre-build assert is the missing guardrail.",
          "CHANGELOG FINALIZE IS DONE POST-HOC BY CI, NOT THE HUMAN. release.yml:251-258 (`Stamp CHANGELOG.md`) runs `sed` AFTER the tag exists, turning `## [Unreleased]` into `## [VERSION] - DATE` and inserting a fresh `[Unreleased]`, then commits it back to main (lines 260-267). Pros: zero manual toil, date is always correct/UTC, can't forget. Cons: (a) the release-notes extraction (release.yml:107-121) reads `[Unreleased]` from the TAGGED commit, so whatever you forgot to write before tagging is what ships — CI can't invent notes; (b) it pushes a bot commit to main from a tag build, which races concurrent merges and won't re-trigger CI on that commit; (c) the stamp is brittle sed with no idempotency guard (re-running a tag double-stamps). Recommended hybrid: human curates the Unreleased bullets and DATE-less heading before tagging; CI only stamps the date + opens a fresh Unreleased. This is exactly ripgrep's and gh-cli's split (human writes notes, automation handles mechanics).",
          "THE `-rcN` (NO-DOT) PRERELEASE SCHEME IS A SEMVER ORDERING FOOTGUN. am tags are `v0.5.0-rc7`. Per SemVer item 11, `rc7` is a SINGLE alphanumeric identifier compared in ASCII order, so `1.0.0-rc10 < 1.0.0-rc7 < 1.0.0-rc8` (verified: ASCII sort yields rc10, rc11, rc6, rc7, rc8, rc9). Today the installer dodges this by fetching `/releases?per_page=1` (newest by created_at, not semver) — correct per repo comment — but ANY true-semver consumer (GitHub `make_latest: legacy`, Homebrew/livecheck, dependabot, a future `npm dist-tag`, or sorting tags with `sort -V`/node-semver) will mis-rank rc8+ vs rc10+. Fix going forward: use the DOTTED form `-rc.7` (spec example `1.0.0-rc.1`), where the numeric identifier `7` compares NUMERICALLY. Ordering then matches human intent: `rc.7 < rc.8 < rc.10 < rc.11 < (stable)`.",
          "GITHUB RELEASE 'LATEST' SEMANTICS — repo's installer comment is CORRECT and the prerelease flag in release.yml is right. GitHub `GET /releases/latest` returns 'the most recent non-prerelease, non-draft release, sorted by created_at'; `gh release create --prerelease` makes a release ineligible to be 'Latest'. release.yml:130-133 correctly sets `--prerelease` when the tag matches `-(rc|alpha|beta)`. Because every am tag so far is an rc, `/releases/latest` would return a STALE non-rc tag (or 404), which is why install.sh:140-161 prefers `/releases?per_page=1` (any kind, newest-first) and falls back to `/releases/latest`. When 1.0 ships non-prerelease, it becomes both newest-by-created_at AND Latest — no installer change needed.",
          "RIPGREP'S CHECKLIST GIVES THE MISSING SAFETY ORDER: push the prep commit to the default branch WITHOUT the tag, wait for branch CI to go green, THEN push the tag. am currently does both at once (bump-version.sh:119-120 pushes HEAD then the tag back-to-back) and has no pre-tag CI gate — so a broken build is only discovered after the tag exists, forcing a delete-tag-and-retag dance. Adopting the two-phase push (or gating the release workflow's build on a fresh CI run of the tagged SHA) removes mislabeled/failed-build releases.",
          "GH-CLI AND RIPGREP BOTH USE A SINGLE DRIVER SCRIPT + A 'WHEN IT GOES WRONG' SECTION. gh: `script/release vX.Y.Z`, maintainer-gated deploy, notes auto-generated from merged PRs, recovery = delete release+tag, re-release, fix Homebrew SHA. ripgrep: sync master → finalize CHANGELOG → bump → `cargo package` (dry-run verify) → push commits, wait for CI, push tag → copy CHANGELOG into release notes → recovery = delete tag+draft, re-tag. am already HAS the driver (`scripts/bump-version.sh`); RELEASING.md should document it plus a recovery section (delete the GitHub release AND the tag, since the workflow already pushed a formula/CHANGELOG commit to main that must be reverted too).",
          "KEEP-A-CHANGELOG 1.1.0 EXACT FORMAT: versioned heading is `## [1.1.1] - 2023-03-05` (ISO-8601 date), categories are Added/Changed/Deprecated/Removed/Fixed/Security, and the recommended (currently ABSENT in am's CHANGELOG) footer is reference-style compare links: `[unreleased]: .../compare/v1.1.1...HEAD`, `[1.1.1]: .../compare/v1.1.0...v1.1.1`, `[1.0.0]: .../releases/tag/v1.0.0`. am's CI stamp omits these links; adding them (and stamping them in CI) makes every version one click from its diff."
        ],
        "recommendedApproach": "RECIPE — two deliverables.\\n\\n=== (1) release.yml VERSION-ASSERT STEP ===\\nAdd a fast gating job that fails the whole workflow before any binary is built, so package.json, the CHANGELOG, and the tag can never disagree. Put it as a job that `build` depends on (`needs: assert-version`).\\n\\n```yaml\\n  assert-version:\\n    runs-on: blacksmith-2vcpu-ubuntu-2404\\n    steps:\\n      - uses: actions/checkout@v4\\n      - uses: useblacksmith/setup-bun@v1\\n        with: { bun-version: 1.3.11 }\\n      - name: Assert tag == package.json version (and CHANGELOG is ready)\\n        shell: bash\\n        run: |\\n          set -euo pipefail\\n          TAG=\\\"$GITHUB_REF_NAME\\\"            # e.g. v0.5.0-rc.8 (runner-set, trusted)\\n          WANT=\\\"${TAG#v}\\\"                    # 0.5.0-rc.8\\n          PKG=\\\"$(bun -e \\\"console.log((await Bun.file('package.json').json()).version)\\\")\\\"\\n          if [ \\\"$WANT\\\" != \\\"$PKG\\\" ]; then\\n            echo \\\"::error::tag $TAG implies version $WANT but package.json is $PKG\\\"\\n            echo \\\"Run: ./scripts/bump-version.sh $WANT   (bumps package.json, commits, tags, pushes)\\\"\\n            exit 1\\n          fi\\n          # SemVer shape guard (X.Y.Z with optional DOTTED prerelease: -rc.N / -alpha.N / -beta.N)\\n          if ! printf '%s' \\\"$WANT\\\" | grep -Eq '^[0-9]+\\\\.[0-9]+\\\\.[0-9]+(-(rc|alpha|beta)\\\\.[0-9]+)?$'; then\\n            echo \\\"::error::version '$WANT' is not X.Y.Z[-rc.N]; use the dotted prerelease form so SemVer ordering is numeric\\\"\\n            exit 1\\n          fi\\n          # CHANGELOG must have content staged under [Unreleased] (notes ship from the tagged commit)\\n          NOTES=$(sed -n '/^## \\\\[Unreleased\\\\]/,/^## \\\\[/{/^## \\\\[/d;p}' CHANGELOG.md | grep -v '^[[:space:]]*$' || true)\\n          if [ -z \\\"$NOTES\\\" ]; then\\n            echo \\\"::error::CHANGELOG [Unreleased] is empty — write release notes before tagging\\\"\\n            exit 1\\n          fi\\n          echo \\\"OK: $TAG matches package.json ($PKG) and CHANGELOG has notes\\\"\\n```\\nThen: `build:\\\\n    needs: assert-version` and `release:\\\\n    needs: build` (unchanged). Optional belt-and-suspenders: after `Build targets`, also assert the COMPILED binary self-reports the tag — `./dist/am-linux-x64 version` on the linux runner must contain \\\"$WANT\\\" (catches a build that silently kept 0.0.0-dev). This closes the gap that the build trusts GITHUB_REF_NAME but nothing checks package.json/CHANGELOG.\\n\\nKeep CI's existing `Stamp CHANGELOG.md` step but make it idempotent and link-aware (only stamp if `## [Unreleased]` still has no date below it; also rewrite/append the footer compare links). Bump scripts/bump-version.sh's regex from `[0-9]*.[0-9]*.[0-9]*` to the same dotted-prerelease ERE above so the local driver rejects `-rc7` and old-style typos.\\n\\n=== (2) RELEASING.md CONTENT (drop at repo root; pointer from AGENTS.md) ===\\nStructure it as a copy-pasteable checklist with a recovery section (modeled on ripgrep + gh-cli, adapted to am's git-backed/CI-stamped flow):\\n\\n# Releasing agent-manager (am)\\nDistribution: GitHub Releases (5 platform binaries + checksums.sha256, consumed by install.sh) and Homebrew. npm is DEFERRED (unscoped name taken; see CHANGELOG note). A tag `vX.Y.Z[-rc.N]` triggers .github/workflows/release.yml.\\n\\n## Versioning\\n- Stable: `vX.Y.Z`. Prerelease: `vX.Y.Z-rc.N` (DOTTED — `-rc.8`, NOT `-rc8`). The dot makes N a numeric SemVer identifier so rc.8 < rc.10; the no-dot form sorts rc.10 < rc.7 in ASCII and breaks every semver-aware tool.\\n- The build bakes the version from the git tag (GITHUB_REF_NAME), so the tag is the source of truth. package.json must match it (CI asserts this).\\n\\n## Pre-flight (on an up-to-date main)\\n1. `git switch main && git pull --ff-only`\\n2. `bun install --frozen-lockfile && bun test && bun run typecheck && bun run lint`  (green or stop)\\n3. Curate CHANGELOG.md: ensure everything since the last tag is under `## [Unreleased]` in Added/Changed/Fixed/etc. Do NOT add the date or version — CI stamps those. Leave the bullets you want as release notes here (they ship verbatim).\\n4. Smoke-build locally: `VERSION=X.Y.Z-rc.N bun run build -- --target bun-linux-x64 && ./dist/am-linux-x64 version` → must print X.Y.Z-rc.N.\\n\\n## Cut the release\\n5. `./scripts/bump-version.sh X.Y.Z-rc.N --dry-run` then without --dry-run. (It checks the tree is clean, the tag is unused, writes package.json, commits `chore: bump version to …`, annotates tag `vX.Y.Z-rc.N`, and pushes commit+tag.)\\n   - SAFER VARIANT (recommended, per ripgrep): push the bump commit first, wait for main CI green, THEN push the tag: `git push origin HEAD` → watch CI → `git push origin vX.Y.Z-rc.N`.\\n\\n## What CI does (do not do by hand)\\n- assert-version: tag == package.json, SemVer shape, [Unreleased] non-empty.\\n- Builds 5 binaries + am-acp-shell, ad-hoc-signs darwin, generates checksums.sha256.\\n- Extracts [Unreleased] as release notes; creates the GitHub Release (auto `--prerelease` for -rc/-alpha/-beta so it is NOT marked Latest).\\n- Regenerates Formula/am.rb, stamps CHANGELOG ([Unreleased] → [X.Y.Z] - DATE + fresh [Unreleased] + footer compare links), commits both back to main.\\n\\n## Verify (after the run is green)\\n6. `curl -fsSL https://raw.githubusercontent.com/Codeseys-Labs/agent-manager/main/install.sh | sh` on a clean machine (or `--dry-run`), then `am version` == X.Y.Z-rc.N.\\n7. Confirm the Release page lists 10 binaries + checksums.sha256, and `--prerelease` is set for rc tags (Latest unchanged).\\n8. Confirm the CI bot commit landed on main (package.json, CHANGELOG dated, Formula updated).\\n\\n## If it goes wrong (recovery)\\n- Delete the GitHub Release AND the tag (local+remote): `gh release delete vX.Y.Z-rc.N --cleanup-tag` or `git push --delete origin vX.Y.Z-rc.N`.\\n- If CI already pushed the stamp/formula commit to main, revert it: `git revert <sha> && git push`.\\n- Fix the cause, re-bump to the NEXT rc (never re-use a tag), re-push.\\n\\nKeep the file short and link it from AGENTS.md (am's single-source-of-truth doc) rather than duplicating policy.",
        "pitfalls": [
          "Asserting against package.json is necessary but NOT sufficient: the binary's version comes from GITHUB_REF_NAME via --define, so also assert the COMPILED binary self-reports the tag (`./dist/am-linux-x64 version` contains the version) to catch a build that silently fell back to BUILD_VERSION's `0.0.0-dev` sentinel.",
          "Do NOT switch existing published tags from `-rc7` to `-rc.7` retroactively — that rewrites history and breaks the Homebrew formula/install URLs already referencing them. Apply the dotted scheme from the NEXT tag forward only; document the cutover in the CHANGELOG.",
          "CI stamping the CHANGELOG and pushing to main from a tag-triggered run is racy: that bot commit (a) can conflict with a merge that landed between tag-push and workflow-run, and (b) does NOT re-trigger CI on itself. Make the stamp idempotent (guard: skip if [Unreleased] already has no entries or the version heading already exists) so a re-run never double-stamps, and prefer the human curating notes pre-tag (CI cannot author what you forgot).",
          "The release-notes sed (release.yml:112) matches `## [` as the section terminator; a bullet line that happens to start with `## [` (unlikely but possible in nested examples/code fences) would truncate notes. Low risk, but the assert-version `[Unreleased] non-empty` check won't catch malformed notes — eyeball the generated notes on first use of any new format.",
          "`make_latest` defaults to true; relying on the implicit prerelease guard alone is fine, but if a stable `vX.Y.Z` is ever pushed while an rc is the newest by created_at, GitHub will (correctly) flip Latest to the stable one — make sure the installer's `/releases?per_page=1` preference is still desired post-1.0 (it returns newest-of-any-kind, so a freshly pushed rc would out-rank a slightly older stable). After 1.0, consider switching the installer's default to `/releases/latest` and gating rc installs behind an explicit `--version` / `AM_VERSION`.",
          "bump-version.sh's current regex `[0-9]*.[0-9]*.[0-9]*` is too loose (the `.` is a glob `.` matching any char, and it accepts `-rc7`); tighten it to the dotted-prerelease ERE so the local driver can't create a mis-orderable tag in the first place — defense in depth with the CI assert.",
          "Two-phase push (commit, wait, then tag) is the safe order, but bump-version.sh pushes both immediately (lines 119-120). If you adopt the two-phase flow, either add a `--no-tag-push` mode to the script or document the manual `git push origin <tag>` step so the runbook and the tool agree.",
          "The npm `Set version from tag` step (release.yml:279-286) mutates package.json in the workflow but never commits it and the npm publish is disabled — harmless now, but if npm is re-enabled, ensure that mutation can't drift from the asserted version (it reads the same tag, so it will agree, but the dotted-rc form must be a valid npm prerelease tag — it is)."
        ],
        "sources": [
          "/mnt/e/CS/github/agent-manager/.github/workflows/release.yml (build VERSION from GITHUB_REF_NAME L33; release-notes sed L107-121; prerelease flag L130-133; CHANGELOG stamp L251-258; bot commit to main L260-267; npm version-from-tag L279-286)",
          "/mnt/e/CS/github/agent-manager/install.sh (fetch_latest_version prefers /releases?per_page=1 over /releases/latest L140-161; checksum fail-closed L96-119,246-268)",
          "/mnt/e/CS/github/agent-manager/scripts/build.ts (VERSION env default 0.0.0-dev L3; --define BUILD_VERSION L124; 5 targets L11-17)",
          "/mnt/e/CS/github/agent-manager/scripts/bump-version.sh (loose semver regex L49-55; clean-tree + tag-exists guards L61-71; pushes HEAD then tag L119-120)",
          "/mnt/e/CS/github/agent-manager/src/lib/version.ts (AM_VERSION = process.env.BUILD_VERSION ?? '0.0.0-dev')",
          "/mnt/e/CS/github/agent-manager/CHANGELOG.md (Keep-a-Changelog layout, no footer compare links; npm-deferred note L54-56)",
          "Keep a Changelog 1.1.0 — https://keepachangelog.com/en/1.1.0/ (versioned heading `## [1.1.1] - 2023-03-05`; categories; footer compare links; move Unreleased into versioned section on release)",
          "Semantic Versioning 2.0.0 — https://semver.org/ (items 9 & 11: prerelease syntax `1.0.0-rc.1`; precedence rules; numeric vs ASCII identifier ordering)",
          "GitHub CLI release runbook — https://github.com/cli/cli/blob/trunk/docs/releasing.md (script/release vX.Y.Z driver; maintainer-gated deploy; auto release notes; delete-release+tag recovery)",
          "ripgrep RELEASE-CHECKLIST.md — https://github.com/BurntSushi/ripgrep/blob/master/RELEASE-CHECKLIST.md (finalize CHANGELOG before tag; push commit, WAIT for CI, then push tag; delete tag+draft on failure; TBD/Unreleased placeholder after release)",
          "GitHub REST Releases API — https://docs.github.com/en/rest/releases/releases (Get latest release = most recent non-prerelease, non-draft by created_at; create release make_latest true/false/legacy, prerelease/draft flags)",
          "GitHub community discussion 26686 — tag-vs-manifest version assert pattern using github.ref_name (https://github.com/orgs/community/discussions/26686)"
        ]
      }
    ],
    "synthesis": "# agent-manager Backlog Brief — 3 Topics, Implementation-Ready\n\n---\n\n## 1. ServerSchema → discriminatedUnion on `transport` (seed a067)\n**Confidence: HIGH** (all claims empirically probed against the repo's own Zod 3.25.76)\n\n### Final approach\nSingle schema edit in `src/core/schema.ts` + two small constructor fixes. NOT a 13-adapter rewrite.\n\n- **Shape:** `ServerBase` (all common fields incl. `command`, `enabled.default(true)`, `_registry`/`_marketplace`, `adapters`) → `StdioServer = Base.extend({ transport: z.literal(\"stdio\"), url: z.undefined().optional() })` and `RemoteServer = Base.extend({ transport: z.union([literal(\"streamable-http\"), literal(\"sse\")]), url: z.string().optional() })`.\n- **Keep `command` on BOTH variants** — am stores the remote URL in `command` (export-utils.ts:95). This is what decouples the union from every adapter read.\n- **Default handling via `z.preprocess`, NOT `.default()` on the literal.** Wrap the union: inject `transport:\"stdio\"` when the key is absent (guard `v && typeof v === \"object\"` first). The union resolver reads raw input before field defaults, so `.default(\"stdio\")` on the literal still throws `\"No matching discriminator\"` at runtime — verified.\n- **`url`-on-stdio rejection is now structural** (`z.undefined()`), replacing that superRefine branch. Re-attach only the `_registry` XOR `_marketplace` refine via `.superRefine` on the union (inside preprocess).\n- **Adapters: zero changes** — they consume `ResolvedServer` (hand-written, decoupled from `z.infer<Server>`). `buildResolvedConfig` (config.ts:213-224) typechecks unchanged.\n- **Fix exactly 2 sites** — `install.ts:156-174` and `marketplace/installer.ts:157-172` use object-literal-then-`server.url=` mutation (fails TS2322 under the union). Fold `url` into the literal via conditional spread, branched on transport. `add.ts:154` / `init-project.ts:101` are safe (literal `'stdio'`, mutate only shared fields).\n\n### Top pitfalls\n1. **`.default()` on the discriminator literal passes typecheck but ships broken** — runtime still throws on transport-absent config.toml. The preprocess injection is load-bearing; this is the #1 way to ship a green-typecheck broken migration.\n2. **Don't move `command` out of remote** — breaks export-utils, mcp-superset.ts:112, url-credentials.ts:107, and the schema.test.ts:135 \"accepts url on remote\" case.\n3. **`z.preprocess` makes the outer schema `ZodEffects`** — `.shape`/`.extend`/`.merge`/`.pick`/`.omit` break. Current usage is only `.parse`/`.safeParse` + `z.record(z.string(), ServerSchema)` — all fine. Expose the inner union separately if future code needs `.shape`.\n4. **stdio+url error message changes** (custom string → Zod `invalid_type` on `[\"url\"]`). Current test only asserts `.toThrow()` so it passes; grep for the old message string before merge.\n5. Resist `as Server`/`as any` to silence the 2 mutate sites — it reintroduces the exact runtime bug the union prevents.\n\n### Verification gate\n`bun run scripts/typecheck.ts` && `bun test test/core/schema.test.ts test/core test/adapters test/commands/install.test.ts`\n\n---\n\n## 2. Injectable clock seam for LRUCache TTL testing (seed 06d8)\n**Confidence: HIGH** (seam pattern verified; Bun behavior empirically confirmed on 1.3.14)\n\n### Final approach\nConstructor-injected `now()` function in `src/registry/client.ts` (LRUCache L10-38). Three edits, zero call-site churn:\n\n1. Add defaulted 3rd param: `private now: () => number = Date.now` (pass the reference, not `() => Date.now()`).\n2. L20: `Date.now() > entry.expiresAt` → `this.now() > entry.expiresAt`.\n3. L36: `Date.now() + this.ttlMs` → `this.now() + this.ttlMs`.\n\n**Export the class** (`export class LRUCache<T>`) so tests construct fresh instances with a fake clock — do NOT export the module singleton `cache`. Tests advance time by mutating a captured `let clock`, no globals, no afterEach restore.\n\nThen **rewrite the existing TTL test** (client-resilience.test.ts:211-235): delete the `Date.now = () => fakeNow` monkey-patch + the per-test unique-URL collision hack (L70-73); cover TTL via a direct LRUCache unit test instead. Leave the resilience file for network/retry/fallback/eviction only. **Keep the separate `setTimeout` stub** (L80-86) for backoff sleeps.\n\nPrefer DI over `setSystemTime` here — you own the code; DI is local, runner-agnostic, no leak. (`setSystemTime` stays the fallback for code you can't refactor.)\n\n### Top pitfalls\n1. **Boundary off-by-one:** guard is strict `>`, so the entry is valid at *exactly* `expiresAt` and dies one ms later. Advance by `ttlMs + 1` to cross; assert both edges.\n2. **`setSystemTime` does NOT mock `setTimeout`/`setInterval`** (Bun: \"Timers are not impacted yet\"). Don't delete the backoff `setTimeout` stub thinking a time mock covers it.\n3. **Keep the param optional/defaulted** — required would break the L53 singleton + any `new LRUCache(max, ttl)` caller.\n4. If you ever migrate to `isaacs/lru-cache`, mirror via its `perf: { now }` option — it captures global Date/performance at *import time*, so plain Date-mocking fails. The hand-rolled cache sidesteps this; don't add the dependency just for this seam.\n\n---\n\n## 3. Release engineering: version-assert + RELEASING.md (npm deferred)\n**Confidence: HIGH** for the assert/CHANGELOG/recovery mechanics; **MEDIUM** for the `-rc.N` cutover (a forward-only policy choice with a documented migration cost)\n\n### Final approach — two deliverables\n\n**(A) `assert-version` job in release.yml** (gates everything; `build: needs: assert-version`, `release: needs: build`):\n- Compare `${GITHUB_REF_NAME#v}` vs `package.json` version → fail with a copy-paste `./scripts/bump-version.sh` hint on mismatch.\n- SemVer shape guard: `^[0-9]+\\.[0-9]+\\.[0-9]+(-(rc|alpha|beta)\\.[0-9]+)?$` (dotted prerelease only).\n- Assert `[Unreleased]` is non-empty (notes ship from the tagged commit — CI can't author what you forgot).\n- **Belt-and-suspenders:** after build, assert the compiled binary self-reports the tag (`./dist/am-linux-x64 version` contains `$WANT`) — catches a silent fallback to the `0.0.0-dev` sentinel.\n\n**(B) RELEASING.md** at repo root, linked from AGENTS.md (don't duplicate policy). Sections: Versioning · Pre-flight (green tests + curate `[Unreleased]` *without* date/version) · Cut the release (`bump-version.sh`, two-phase push) · What CI does · Verify · **Recovery** (`gh release delete --cleanup-tag`; revert the CI bot stamp/formula commit; re-bump to the NEXT rc, never reuse a tag).\n\n**Policy changes:**\n- **Adopt dotted `-rc.N` from the next tag forward.** `-rc7` (no dot) is a single ASCII identifier → `rc.10 < rc.7` for any semver-aware tool. Dotted form compares N numerically. Tighten `bump-version.sh`'s loose `[0-9]*.[0-9]*.[0-9]*` regex to the dotted ERE.\n- **Two-phase push** (push bump commit → wait for main CI green → push tag) instead of bump-version.sh's back-to-back push (L119-120). Add a `--no-tag-push` mode or document the manual `git push origin <tag>`.\n- **CHANGELOG: human curates bullets pre-tag, CI stamps date + opens fresh `[Unreleased]` + footer compare links** (currently absent). Make the stamp idempotent (skip if heading already dated).\n\n### Top pitfalls\n1. **package.json assert alone is insufficient** — the binary version comes from `GITHUB_REF_NAME` via `--define`, independent of package.json. Add the compiled-binary self-report assert.\n2. **Do NOT retroactively rename published `-rc7` → `-rc.7`** — rewrites history, breaks Homebrew formula + install URLs. Forward-only; document the cutover in CHANGELOG.\n3. **CI stamping + pushing to main from a tag run is racy** — the bot commit can conflict with a merge landed between tag-push and run, and won't re-trigger CI on itself. Make the stamp idempotent so re-runs don't double-stamp.\n4. **Installer `/releases?per_page=1` is correct now but reconsider post-1.0** — newest-of-any-kind means a fresh rc out-ranks a slightly older stable. After 1.0, switch the installer default to `/releases/latest` and gate rc installs behind explicit `--version`/`AM_VERSION`.\n5. `make_latest` defaults true; the `--prerelease` flag (release.yml:130-133) correctly keeps rc tags off \"Latest\" — leave it.\n\n---\n\n### Suggested execution order\n1. **a067 (schema)** first — pure-internal, high-confidence, unblocks any further config work; lands as ~1 schema edit + 2 constructor fixes.\n2. **06d8 (clock seam)** in parallel — orthogonal, trivial, removes a global-monkey-patch anti-pattern.\n3. **Release eng** last — the version-assert job + RELEASING.md are independent and don't block code work; the `-rc.N` cutover is a forward-only policy decision worth user signoff before the next tag.\n\nRelevant files: `/mnt/e/CS/github/agent-manager/src/core/schema.ts`, `src/commands/install.ts`, `src/marketplace/installer.ts`, `src/registry/client.ts`, `test/registry/client-resilience.test.ts`, `.github/workflows/release.yml`, `scripts/bump-version.sh`, and new `RELEASING.md`."
  }
}