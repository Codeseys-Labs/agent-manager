# Deep-work-loop backlog — Run 2026-05-05

**Source:** `docs/reviews/2026-05-05-parallel-critique/synthesis.md` + pre-flight router fix.
**Baseline hash:** `488f777`
**Budget:** 3 waves max.
**Scope rule:** items that can't land in 3 waves are marked OUT OF SCOPE with a reason.

## Backlog

| id | pillar | priority | complexity | source | item | file-ownership |
|---|---|---|---|---|---|---|
| B-00 | meta (Hermes) | P0 | S | pre-flight | Fix Hermes `delegate_task` per-task `model`/`provider` ignored | **external: `/home/codeseys/.hermes/hermes-agent/tools/delegate_tool.py`** |
| B-01 | 4 (Marketplace) | P0 | S | synthesis C3 | Marketplace RCE: gate novel `serverDef.command` | `src/marketplace/installer.ts`, `src/marketplace/security.ts`, `test/marketplace/installer.security.test.ts` (new) |
| B-02 | 4 (Marketplace) | P0 | M | synthesis C3 / D1 | Decision ADR: commit to marketplace v1 or retire ADR-0034/0035 | `ADRs/0039-marketplace-v1-scope-decision.md` (new) |
| B-03 | meta (sec hygiene) | P0 | S | synthesis Union (security) | Community adapter env sandbox propagation | `src/adapters/community/proxy.ts`, `test/adapters/community/proxy.env.test.ts` (new) |
| B-04 | cross-cutting | P1 | S | synthesis C4 | Amend ADR-0031 pillar 6 ("local write paths", not "three UIs over one core") | `ADRs/0031-product-scope-and-pillars.md`, `AGENTS.md` |
| B-05 | cross-cutting | P1 | S | synthesis Union (arch) | New ADR: controller scope + concurrency model | `ADRs/0040-controller-scope-and-concurrency.md` (new) |
| B-06 | cross-cutting | P1 | M | synthesis C2 | ADR-0007 Phase 2 resolution — decide + execute | `ADRs/0007-two-phase-zod-validation.md`; one of: `src/core/config.ts` (wire) OR adapter `schema.ts` files + `src/adapters/types.ts` (delete) |
| B-07 | 5 (wiki) | P1 | S | synthesis Union (security) | Wiki path-traversal fix on `/api/wiki/pages/:slug` | `src/web/server.ts`, `test/web/wiki.path-traversal.test.ts` (new) |
| B-08 | 4 (docs) | P1 | S | synthesis Union (security) | SECURITY.md plaintext-downstream note | `SECURITY.md` |
| B-09 | 6 (web) | P2 | S | synthesis Union (security) | `Bun.serve` default bind loopback | `src/web/server.ts`, `test/web/server.hostname.test.ts` (new) |
| B-10 | 6 (CF Worker) | P2 | S | synthesis Union (security) | CF Worker HKDF per-session salt | `src/web/worker.ts` |
| B-11 | meta (sec hygiene) | P2 | M | synthesis Union (security) | Secret-detection tier-1.5: wire value-shape regexes | `src/core/secret-detection.ts`, `src/lib/redact.ts`, new tests |
| B-12 | cross-cutting | P2 | S | synthesis Union (arch) | ADR-0027 community adapters auto-run gap (`getDetectedAdapters`) | `src/adapters/registry.ts` |
| B-13 | cross-cutting | P2 | S | synthesis C5 | Terminology / count drift (counts generated from code) | `AGENTS.md`, `scripts/gen-count.ts` (new) |

## OUT OF SCOPE for this run (deferred)

| id | item | reason |
|---|---|---|
| OOS-1 | Megafile split (`mcp/server.ts` 3245 LOC, `commands/wiki.ts` 1361) | Synthesis explicitly recommends waiting for real cross-family review signal before committing 1 week of refactor. |
| OOS-2 | Pillar 5 SessionReader for 6 more adapters | Vision-lens only finding; XL effort; not a convergent signal. |
| OOS-3 | Wiki CLI 17→5 verbs | Same as OOS-2. |
| OOS-4 | am wiki distill --as-skill | Integration-lens only; good idea but not urgent. |
| OOS-5 | Hermes-as-am-marketplace | Speculative; depends on B-02 decision. |
| OOS-6 | Promote/close ADRs 0034–0038 | Subsumed by B-02 for 0034-35; 0036-38 need separate decision cycles. |
| OOS-7 | Missing tests beyond the wave-1 security ones | Track in backlog; not required for this run's success criteria. |
| OOS-8 | Flows vs ADR-0031 contradiction | Separate design decision; not addressable in a wave. |

## Wave plan

### Wave 1 — Pre-flight + P0 security (parallel, no file overlap)

| id | file ownership |
|---|---|
| B-00 | external (`~/.hermes/hermes-agent/tools/delegate_tool.py`) |
| B-01 | `src/marketplace/installer.ts`, `src/marketplace/security.ts`, `test/marketplace/installer.security.test.ts` |
| B-03 | `src/adapters/community/proxy.ts`, `test/adapters/community/proxy.env.test.ts` |
| B-07 | `src/web/server.ts`, `test/web/wiki.path-traversal.test.ts` |

Rationale:
- B-00 must land first — without it, Wave 1 final review can't use different families.
- B-01, B-03, B-07 all touch disjoint files. Safe to parallelize.
- B-02 (decision ADR) deferred to Wave 2 after B-01 lands — the security fix is a precondition for arguing either "keep marketplace" (safer) or "retire it" (security fix isolates risk if we go that way).

Wave 1 acceptance (executable, all must pass):
```
bun test                              # 2662+ passing
bun run typecheck                     # 0 errors
bun run lint                          # 0 errors
# B-00 verified: delegate_task(tasks=[{model:'google/gemini-3.1-pro-preview', ...}]) returns with gemini in metadata.model
# B-01 verified: new installer.security.test.ts passes
# B-03 verified: new proxy.env.test.ts passes
# B-07 verified: new wiki.path-traversal.test.ts passes
```

### Wave 2 — ADR hygiene + decision ADRs (serialized — all touch ADRs dir)

| id | file ownership |
|---|---|
| B-02 | `ADRs/0039-marketplace-v1-scope-decision.md` (new) |
| B-04 | `ADRs/0031-product-scope-and-pillars.md`, `AGENTS.md` |
| B-05 | `ADRs/0040-controller-scope-and-concurrency.md` (new) |
| B-06 | `ADRs/0007-two-phase-zod-validation.md` + code changes (TBD based on decision) |
| B-08 | `SECURITY.md` |

Rationale:
- Intentionally sequential. Every item touches ADRs/ or top-level docs. Parallel execution here causes merge conflicts.
- B-02 depends on B-01 landing (security fix validates "marketplace CAN be safe").
- B-04 can run first since it's purely documentation.

### Wave 3 — P2s if budget remains

B-09, B-10, B-11, B-12, B-13. Each is S effort. Parallelize freely.

## Non-goals for this run (explicit)

- Writing user-facing features.
- Refactoring non-security files that the review didn't flag.
- Changing MCP gateway / ACP / A2A behavior unless required by a P0 item.
- Building `am` against a new model in the roster.

## Phase 8 (cross-family review) plan

After Wave 1 lands, use `delegate_task` with per-task `model` overrides (now working post-B-00) to fan out across **three genuinely different families**:

- `openai/gpt-5.5` — OpenAI family
- `google/gemini-3.1-pro-preview` — Google family
- `deepseek/deepseek-v4-pro` — DeepSeek family

If B-00 was the only router fix needed, these three will return with their own model tags in metadata. If they still come back as Opus, B-00 is incomplete and we loop.
